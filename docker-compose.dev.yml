services:
  db:
    image: postgres:17
    container_name: database
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - ./postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: